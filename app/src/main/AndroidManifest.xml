<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devsoc.tictactoe">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar.FullScreen">
        <activity android:name="com.devsoc.tictactoe.activities.StartGameActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="com.devsoc.tictactoe.activities.MainActivityPassAndPlay" />
        <activity
            android:name="com.devsoc.tictactoe.activities.SettingsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.devsoc.tictactoe.activities.MainActivityPlayOnline"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.devsoc.tictactoe.activities.SplashScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>